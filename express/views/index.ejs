<%- include('includes/head', { title: 'Home' }) %>
<main class="flex flex-col items-center min-h-screen py-10">
	<section
		id="posts"
		class="grid grid-cols-3 max-w-6xl w-full rounded-box gap-6 p-6"
	></section>
</main>

<script>
	function getRandomPost(count = 1) {
		for (let i = 0; i < count; i++) {
			fetch(`/post/random`)
				.then((response) => response.text())
				.then((data) => {
					posts.innerHTML += data;
				})
				.catch((error) => {
					console.error("Error fetching posts:", error);
				});
		}
	}

	document.addEventListener("DOMContentLoaded", getRandomPost(6));

	window.addEventListener("scroll", () => {
		if (window.innerHeight + window.scrollY >= document.body.offsetHeight) {
			getRandomPost(3);
		}
	});
</script>
